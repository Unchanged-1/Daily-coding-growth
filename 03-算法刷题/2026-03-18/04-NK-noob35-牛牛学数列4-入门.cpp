/*
 * 题目：牛牛学数列4
 * 来源：牛客网（noob35）
 * 知识类型：C++基础语法 | 双层循环（嵌套for） | 数列求和（嵌套求和） | 数学公式优化（三重求和转单公式） | 整数运算
 * 题目原文：
 *   描述：计算嵌套数列和：1 + (1+2) + (1+2+3) + … + (1+2+…+n)（1≦n≦100）。
 *   输入描述：一行输入整数n（1≦n≦100）。
 *   输出描述：输出求和结果（整数）。
 *   示例1：输入4 → 输出20（1 + 3 + 6 + 10 = 20）。
 *   难度：入门，通过率63.05%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 正确计算嵌套数列的和；
 *   2. 输出最终整数结果。
 * 解题思路（双层循环法，新手友好）：
 *   1. 外层循环遍历1~n（对应每个括号内的求和项）；
 *   2. 内层循环计算每个括号内的和（1+2+…+i）；
 *   3. 累加所有内层和得到最终结果。
 */
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, total = 0;
    cin >> n;
    // 外层循环：遍历每个嵌套项（1~n）
    for (int i = 1; i <= n; i++)
    {
        int sum = 0;
        // 内层循环：计算1+2+…+i的和
        for (int j = 1; j <= i; j++)
        {
            sum += j;
        }
        total += sum; // 累加每个嵌套项的和
    }
    cout << total << endl;
    return 0;
}

/* 可选优化写法（数学公式法，O(1)时间复杂度，无循环）：
#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    // 核心公式：嵌套求和 = n*(n+1)*(n+2)/6
    // 推导：1+(1+2)+…+(1+2+…+n) = Σ(1~n)[i(i+1)/2] = n(n+1)(n+2)/6
    int total = n * (n + 1) * (n + 2) / 6;
    cout<<total<<endl;
    return 0;
}

// 中间优化：单层循环（先算每个项的公式和，再累加）
#include<bits/stdc++.h>
using namespace std;

int main() {
    int n,total=0;
    cin>>n;
    for(int i=1;i<=n;i++){
        total += i*(i+1)/2; // 用高斯公式算1+2+…+i，替代内层循环
    }
    cout<<total<<endl;
    return 0;
}
*/

/*
======= 核心重点 & 易错点 =======
【重点】
1. 双层循环逻辑（新手核心）：
   - 外层i：控制嵌套项的个数（第i项是1+2+…+i）；
   - 内层j：计算单个嵌套项的和，累加后存入total；
2. 数学公式优化（核心规律）：
   - 单个嵌套项和：1+2+…+i = i(i+1)/2（高斯公式）；
   - 整体嵌套和：Σ(1~n)[i(i+1)/2] = n(n+1)(n+2)/6（三重求和化简）；
   - 验证：n=4时，4×5×6/6=20，和示例结果一致；
3. 数据范围：n≤100时，n(n+1)(n+2)/6 = 100×101×102/6=171700 < int最大值，无溢出；
4. 运算顺序：乘法先算（保证整除，如4×5×6=120，120/6=20）。

【易错点】
1. 循环逻辑错误：
   - 内层循环j从0开始（多加0，sum结果正确但逻辑冗余）；
   - 外层循环i<n（漏算第n项，如n=4时只算到1+3+6=10，结果错误）；
2. 公式法易错点：
   - 公式写错（如写成n(n+1)(n-1)/6，结果完全错误）；
   - 运算顺序错误（先除后乘，如n=4时4/6×5×6=20，但n=5时5/6×6×7=35？不，整数除法5/6=0，结果为0，完全错误）；
3. 变量初始化错误：
   - total未初始化（取随机值，结果错误）；
   - sum未在每次外层循环初始化（累加前一次的sum，如i=2时sum=1+1+2=4，错误）。
*/