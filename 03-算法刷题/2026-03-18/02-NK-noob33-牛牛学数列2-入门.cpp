/*
 * 题目：牛牛学数列2
 * 来源：牛客网（noob33）
 * 知识类型：C++基础语法 | 循环结构（for循环） | 数列求和（调和级数） | 浮点运算 | 输出格式控制（固定小数位数）
 * 题目原文：
 *   描述：计算调和级数前n项和 H(n) = 1 + 1/2 + 1/3 + … + 1/n（1≦n≦1000）。
 *   输入描述：一行输入整数n（1≦n≦1000）。
 *   输出描述：输出浮点数，保留6位小数（误差≤10⁻⁵即正确）。
 *   示例1：输入2 → 输出1.500000（1+1/2=1.5）。
 *   难度：入门，通过率44.81%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 正确计算调和级数和（浮点运算，避免整数除法截断）；
 *   2. 输出保留6位小数，格式符合要求。
 * 解题思路（循环法，唯一可行解法）：
 *   1. 初始化浮点型sum=0；
 *   2. 循环i从1到n，累加1.0/i（浮点除法）；
 *   3. 用fixed+setprecision(6)控制输出6位小数。
 */
#include <bits/stdc++.h>
using namespace std;
int main()
{
    double n, sum = 0;
    cin >> n;
    // 循环累加调和级数项：1/1 + 1/2 + ... + 1/n
    for (double i = 1; i <= n; i++)
    {
        sum += 1 / i; // i是double类型，1/i为浮点除法，避免截断
    }
    // fixed：固定小数位；setprecision(6)：保留6位小数（匹配示例输出）
    cout << fixed << setprecision(6) << sum << endl;
    return 0;
}

/* 小优化：i用int类型（更贴合n是整数的逻辑，结果一致）
#include<bits/stdc++.h>
using namespace std;
int main() {
    int n;          // n是整数，用int更合理
    double sum=0;
    cin>>n;
    for(int i=1;i<=n;i++){
        sum += 1.0/i;  // 1.0转浮点，避免int/int的整数除法（1/i会变成0）
    }
    cout<<fixed<<setprecision(6)<<sum<<endl;
    return 0;
}
*/

/*
======= 核心重点 & 易错点 =======
【重点】
1. 浮点运算（通过率低的核心原因）：
   - 必须用浮点类型计算（double/float），否则1/i（int类型）会被截断为0（如i=2时，1/2=0）；
   - 两种正确写法：i为double（1/i）、i为int（1.0/i），本质都是浮点除法；
2. 输出格式：
   - fixed：强制以固定小数形式输出（而非科学计数法）；
   - setprecision(6)：严格保留6位小数（示例输出1.500000，需补零）；
3. 调和级数特性：无通用精确数学公式（只能循环累加），n≤1000时循环无性能问题。

【易错点】
1. 整数除法陷阱：
   - 写成int i; sum += 1/i（1/i是int除法，i≥2时结果为0，总和始终为1）；
2. 输出格式错误：
   - 漏写fixed（输出1.5而非1.500000）；
   - setprecision(6)写成setprecision(5)（小数位数不足）；
3. 变量类型错误：
   - sum用int类型（累加浮点值会被截断，结果错误）；
4. 循环范围错误：i从0开始（1/0会导致程序崩溃）。
*/