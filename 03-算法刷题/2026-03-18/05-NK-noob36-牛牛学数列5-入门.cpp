
/*
 * 题目：牛牛学数列5
 * 来源：牛客网（noob36）
 * 知识类型：C++基础语法 | 数组操作 | 斐波那契数列（迭代法） | 空间优化 | 递归与迭代对比
 * 题目原文：
 *   描述：斐波那契数列定义：F₁=1、F₂=1，n≥3时Fₙ=Fₙ₋₁+Fₙ₋₂，计算第n项值（1≦n≦46）。
 *   输入描述：一行输入整数n（1≦n≦46）。
 *   输出描述：输出第n项斐波那契数（整数）。
 *   示例1：输入2 → 输出1；示例2：输入6 → 输出8（数列：1,1,2,3,5,8）。
 *   难度：入门，通过率51.62%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 正确计算斐波那契数列第n项；
 *   2. 适配n≤46的范围（避免溢出/超时）。
 * 解题思路（数组迭代法，新手友好）：
 *   1. 定义数组存储斐波那契数，初始化前两项F₁=1、F₂=1；
 *   2. 循环从3到n，依次计算每一项（Fᵢ=Fᵢ₋₁+Fᵢ₋₂）；
 *   3. 输出数组第n项的值。
 */
#include <bits/stdc++.h>
using namespace std;

int main()
{
   int n, a[47]; // 数组大小47，适配n≤46（下标1~46）
   cin >> n;
   a[1] = 1;
   a[2] = 1; // 初始化前两项
   // 迭代计算第3~n项
   for (int i = 3; i <= n; i++)
   {
      a[i] = a[i - 1] + a[i - 2];
   }
   cout << a[n] << endl; // 输出第n项
   return 0;
}

/* 可选优化写法（空间优化版，无需数组）：
#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    // 边界处理：n=1/n=2直接输出1
    if(n <= 2){
        cout<<1<<endl;
        return 0;
    }
    // 用两个变量存储前两项，替代数组（空间复杂度从O(n)降为O(1)）
    int prev_prev = 1; // Fᵢ₋₂
    int prev = 1;      // Fᵢ₋₁
    int curr;          // Fᵢ
    for(int i=3;i<=n;i++){
        curr = prev + prev_prev;
        prev_prev = prev; // 更新前前项
        prev = curr;      // 更新前项
    }
    cout<<curr<<endl;
    return 0;
}

// 不推荐：递归法（n=46时会重复计算，超时/栈溢出）
// #include<bits/stdc++.h>
// using namespace std;
// int fib(int n) {
//     if(n<=2) return 1;
//     return fib(n-1) + fib(n-2);
// }
// int main() {
//     int n;
//     cin>>n;
//     cout<<fib(n)<<endl;
//     return 0;
// }
*/

/*
======= 核心重点 & 易错点 =======
【重点】
1. 数组迭代法核心（你的代码逻辑）：
   - 数组下标从1开始（贴合题目F₁/F₂的定义，更易理解）；
   - 初始化前两项为1（斐波那契数列的基础）；
   - 循环从3到n（依次推导每一项，无重复计算）；
2. 空间优化逻辑：
   - 斐波那契数仅依赖前两项，无需存储整个数组，用两个变量即可；
   - 空间复杂度从O(n)降为O(1)，n=46时优化效果不明显，但n很大时优势显著；
3. 数据范围关键：
   - n≤46：F₄₆=1836311903 < int最大值（2147483647），无溢出；
   - n>46：需用long long，否则会溢出；
4. 边界处理：n=1/n=2时直接返回1，无需进入循环。

【易错点】
1. 数组/下标错误：
   - 数组开太小（如int a[46];，n=46时a[46]越界）；
   - 下标从0开始（a[0]=1、a[1]=1，输出a[n-1]，易混淆）；
2. 循环逻辑错误：
   - 循环从2开始（重复计算F₂，结果正确但冗余）；
   - 循环i<n（漏算第n项，如n=6时只算到F₅=5，输出错误）；
3. 递归陷阱：
   - 用递归法实现（n=46时会产生大量重复计算，超时/栈溢出，绝对不推荐）；
4. 初始化错误：
   - a[1]或a[2]初始化为0（后续所有项都错误，如F₃=0+0=0）；
5. 变量类型错误：
   - 用short类型（F₄₆远超short范围，溢出）。
*/