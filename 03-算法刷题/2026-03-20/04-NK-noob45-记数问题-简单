/*
 * 题目：记数问题
 * 来源：牛客网（noob45）
 * 知识类型：C++基础语法 | 循环结构 | 数字拆分 | 计数统计 | 边界处理
 * 题目原文：
 *   描述：计算在区间[1, n]的所有整数中，数字x（0≦x≦9）共出现的次数。
 *   输入描述：
 *     一行输入两个整数n, x，用空格隔开。其中1≦n≦10⁶，0≦x≦9。
 *   输出描述：
 *     输出一个整数，表示数字x在区间[1, n]中出现的次数。
 *   示例1：
 *     输入：11 1
 *     输出：4
 *     说明：1~11中数字1出现在1、10、11（两次），共4次。
 *   示例2：
 *     输入：20 1
 *     输出：12
 *     说明：1~20中数字1共出现12次。
 *   难度：简单，通过率48.02%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 准确拆分每个数字的每一位，统计目标数字出现次数；
 *   2. 处理x=0的边界情况，避免无效统计；
 *   3. 保证时间复杂度在合理范围，满足n≤1e6的时限要求；
 *   4. 输入顺序（n在前、x在后）需严格匹配，避免逻辑错误。
 * 解题思路：
 *   1. 读入n和x，初始化计数变量cnt=0；
 *   2. 遍历区间[1, n]的每个整数i；
 *   3. 对每个i进行数字拆分（取模10获最后一位，整除10去掉最后一位）；
 *   4. 每一位与x比对，匹配则cnt加1；
 *   5. 遍历结束后输出cnt的值。
 */
#include <bits/stdc++.h>
using namespace std;

int main() {
    int x,n,cnt=0;
    cin>>n>>x; // 注意输入顺序：先n后x
    for(int i=1;i<=n;i++ ){
        int s = i; // 临时存储当前数字，避免修改循环变量i
        while(s>0){ // 拆分数字的每一位，直到所有位处理完毕
            int d = s%10; // 取当前数字的最后一位
            if(d==x){
                cnt++; // 匹配目标数字，计数+1
            }
            s/=10; // 去掉最后一位，继续处理剩余位
        }
    }
    cout<<cnt<<endl;
    return 0;
}

/*
======= 核心重点 & 易错点 =======
【重点】
1. 数字拆分：通过取模(%)和整除(/)组合拆分每一位，是处理数字位的通用方法；
2. 循环范围：从1开始遍历到n，符合题目区间[1, n]的要求；
3. 变量隔离：用临时变量s存储i，避免修改循环变量导致逻辑错误；
4. 复杂度：O(n*位数)（n≤1e6，最大位数7位），计算量≤7e6，满足1秒时限。

【易错点】
1. 输入顺序：误将x放在n前输入，导致统计对象和区间完全错误；
2. 边界处理：x=0时若遍历从0开始会多统计，需保持i从1开始；
3. 拆分逻辑：while循环条件写成s>=0，导致死循环（s最终会到0，0%10=0，0/10=0）；
4. 计数遗漏：多位数中多次出现x时（如11中的1），需逐位判断，不能只判一次。

【思维扩展】
1. 效率优化解法（数位统计法，空间O(1)）：
   // 核心代码（无需遍历每个数，按位统计，适合n≥1e7的场景）
   int countDigit(int n, int x) {
       int cnt = 0, digit = 1, high = n / 10, cur = n % 10, low = 0;
       while (high != 0 || cur != 0) {
           if (cur < x) cnt += high * digit;
           else if (cur == x) cnt += high * digit + low + 1;
           else cnt += (high + 1) * digit;
           // 处理x=0的特殊情况（最高位不能为0）
           if (x == 0) cnt -= digit;
           low += cur * digit;
           cur = high % 10;
           high /= 10;
           digit *= 10;
       }
       return cnt;
   }
   int main() {
       int n, x;
       cin >> n >> x;
       cout << countDigit(n, x) << endl;
       return 0;
   }
   优势：时间复杂度O(位数)，n=1e6时仅需7次循环，效率远高于遍历法，体现算法优化思维。
*/

/*
个人小结：
1. 数字位处理的核心是取模+整除，是整数位操作的通用技巧；
2. 计数类问题需注意输入顺序、区间边界等细节，避免低级错误；
3. 基础遍历法易实现，数位统计法更高效，可根据数据范围选择。
*/