/*
 * 题目：约瑟夫环
 * 来源：牛客网（noob46）
 * 知识类型：C++基础语法 | 向量(vector)操作 | 循环结构 | 模运算 | 模拟算法
 * 题目原文：
 *   描述：有n个人（编号0∼n−1）围成一圈，从编号为k的人开始报数，报数依次为1,2,…,m，报到m的人出队。下次从出队者的下一个人开始重新报数，直到只剩最后一人，输出该“大王”编号。
 *   输入描述：
 *     一行输入三个整数n(2≦n≦100), k(0≦k≦n−1), m(1≦m≦100)，用空格隔开。
 *   输出描述：
 *     输出一个整数，表示最后剩下的“大王”编号。
 *   示例1：
 *     输入：5 1 2
 *     输出：3
 *     说明：初始队列[0,1,2,3,4]，从1开始报数，依次出队2、4、1、0，最后剩3。
 *   难度：简单，通过率46.91%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 准确模拟报数出队的全过程，符合约瑟夫环规则；
 *   2. 正确计算出队索引和新的起始索引，避免越界；
 *   3. 处理循环报数逻辑，利用模运算适配环形结构；
 *   4. 保证算法效率，满足n≤100的时限要求。
 * 解题思路：
 *   1. 用vector存储0~n-1的编号，模拟初始队列；
 *   2. 初始化起始索引为k；
 *   3. 循环直到队列只剩1个元素：
 *      - 计算出队索引：(起始索引 + m-1) % 队列长度；
 *      - 删除出队元素；
 *      - 更新起始索引为出队索引（模新队列长度防越界）；
 *   4. 输出最后剩余的元素。
 */
#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> p;
    int n,k,m;
    cin>>n>>k>>m;
    // 初始化队列：存储0~n-1的编号
    for(int i=0;i<=n-1;i++){
        p.push_back(i);
    }
    int bg = k; // bg：当前报数的起始索引
    while(p.size()>1){
        // 计算出队索引：m-1是因为报数从1开始，索引从0开始
        int delet = (bg + m-1)%p.size();
        p.erase(p.begin()+delet); // 删除出队元素
        bg = delet %p.size(); // 更新起始索引，模运算防越界
    }
    cout<<p[0]<<endl;
    return 0;
}

/*
======= 核心重点 & 易错点 =======
【重点】
1. 模运算应用：(bg + m-1)%p.size() 解决环形报数的索引越界问题，是核心逻辑；
2. 索引计算：报数从1开始，需减1转换为0起始的索引，避免计数偏差；
3. 起始索引更新：删除元素后，新起始索引为出队索引（模新长度），保证下一轮报数连续；
4. 容器选择：vector的erase操作可直接删除指定索引元素，适配模拟场景。

【易错点】
1. 索引计算错误：未减1直接用(bg + m)%p.size()，导致出队对象偏移；
2. 起始索引更新遗漏：删除元素后未更新bg，或未模新长度导致越界；
3. 初始化错误：队列编号从1开始而非0，与题目要求不符；
4. 循环终止条件：写成p.size()>=1，导致最后一个元素被删除。

【思维扩展】
1. 数学公式解法（空间O(1)，无需模拟）：
   // 核心代码（约瑟夫环递推公式，效率更高）
   int main() {
       int n, k, m;
       cin >> n >> k >> m;
       int ans = 0; // 当n=1时，结果为0
       // 递推公式：f(n) = (f(n-1) + m) % n
       for (int i = 2; i <= n; i++) {
           ans = (ans + m) % i;
       }
       // 调整起始位置（原公式从0开始报数，本题从k开始）
       ans = (ans + k) % n;
       cout << ans << endl;
       return 0;
   }
   优势：空间复杂度从O(n)降为O(1)，时间复杂度O(n)，无需模拟删除操作，适合n更大的场景，体现数学优化思维。
*/

/*
个人小结：
1. 模拟法是解决约瑟夫环的直观思路，核心是模运算处理环形索引；
2. 索引转换（报数1→索引0）是避免计数错误的关键；
3. 递推公式法效率更高，适合数据量较大的场景，需掌握核心递推逻辑。
*/