/*
 * 题目：数位之和
 * 来源：牛客网（noob37）
 * 知识类型：C++基础语法 | 数位操作（取余/整除） | 负数处理（绝对值） | 边界值（0）处理 | 数据范围适配
 * 题目原文：
 *   描述：计算整数n的所有数位之和，负数先取绝对值（-10⁹≦n≦10⁹）。
 *   输入描述：一行输入整数n（-10⁹≦n≦10⁹）。
 *   输出描述：输出数位之和（整数）。
 *   示例1：输入12 → 输出3（1+2=3）；示例2：输入-305 → 输出8（3+0+5=8）。
 *   难度：入门，通过率58.89%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 正确处理负数（取绝对值）；
 *   2. 逐位提取数字并求和；
 *   3. 适配n=0的边界情况。
 * 解题思路（修正版，基于你的代码）：
 *   1. 先将n转为绝对值（处理负数）；
 *   2. 循环取余提取最后一位，累加求和；
 *   3. 整除10去掉最后一位，直到n为0。
 */
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, sum = 0;
    cin >> n;
    n = abs(n); // 核心修正：先取绝对值，处理负数情况
    // 循环提取每一位（n=0时循环终止，自然处理n=0的情况）
    while (n > 0)
    {
        int s = n % 10; // 提取最后一位数字
        sum += s;       // 累加数位和
        n /= 10;        // 去掉最后一位数字
    }
    cout << sum << endl;
    return 0;
}

/* 可选优化写法1（兼容更大范围，用long long）：
#include<bits/stdc++.h>
using namespace std;

int main() {
    long long n; // 适配n>10⁹的情况（题目要求-10⁹~10⁹，int足够，此为拓展）
    int sum=0;
    cin>>n;
    n = llabs(n); // long long版本的绝对值函数
    while(n>0){
        sum += n%10;
        n /= 10;
    }
    cout<<sum<<endl;
    return 0;
}

// 可选优化写法2（字符串法，更直观，无需数学运算）：
#include<bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin>>s;
    int sum=0;
    for(char c : s){
        if(c == '-') continue; // 跳过负号
        sum += c - '0'; // 字符转数字（'0'的ASCII码是48，c-'0'得到对应数字）
    }
    cout<<sum<<endl;
    return 0;
}
*/

/*
======= 核心重点 & 易错点 =======
【重点】
1. 你的代码核心问题（必须修正）：
   - 未处理负数：输入-305时，n=-305不满足n>0，循环不执行，sum=0，输出错误；
   - 补充abs(n)后，负数转为正数，循环正常执行；
2. 数位提取逻辑（核心）：
   - n%10：提取最后一位（如123%10=3，-305%10=-5，取绝对值后为5）；
   - n/=10：去掉最后一位（如123/10=12，305/10=30）；
3. 边界处理：
   - n=0：输入0时，abs(0)=0，循环不执行，sum=0，输出正确（0的数位和为0）；
4. 数据范围：
   - n∈[-10⁹,10⁹]：int可存储（int范围±2×10⁹），无需long long；
   - 数位和最大值：9×9=81（10⁹的数位和为1，999999999的数位和为81），sum用int足够。

【易错点】
1. 负数处理遗漏（通过率低的核心原因）：
   - 忘记abs(n)，负数直接进入循环，n>0不成立，sum=0；
2. 数位提取错误：
   - 先整除后取余（n/=10后再n%10，漏算第一位）；
   - 用n%9替代n%10（完全错误，数位和≠模9结果）；
3. 字符串法易错点：
   - 字符转数字时漏减'0'（直接sum+=c，得到ASCII码，结果错误）；
   - 未跳过负号（sum+= '-' - '0'，得到负数，结果错误）；
4. 边界值n=0：
   - 若手动处理n=0（如if(n==0) sum=0），属于冗余，但不影响结果。
*/