/*
 * 题目：牛牛学数列6
 * 来源：牛客网（noob39）
 * 知识类型：C++基础语法 | 递推数列（迭代法） | 数组操作 | 空间优化 | 边界初始化
 * 题目原文：
 *   描述：数列{Aₙ}定义：A₁=0，A₂=1，A₃=1，n≥4时Aₙ=Aₙ₋₃ + 2×Aₙ₋₂ + Aₙ₋₁，求第n项值（1≦n≦20）。
 *   输入描述：一行输入整数n（1≦n≦20）。
 *   输出描述：输出Aₙ的整数值。
 *   示例1：输入4 → 输出3（A₄=A₁+2×A₂+A₃=0+2×1+1=3）。
 *   难度：入门，通过率60.52%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 正确初始化前三项；
 *   2. 按递推公式计算n≥4的项；
 *   3. 输出第n项结果。
 * 解题思路（数组迭代法，你的核心逻辑）：
 *   1. 定义数组存储数列项，适配n≤20（数组大小22，下标1~20）；
 *   2. 初始化前三项A₁=0、A₂=1、A₃=1；
 *   3. 循环从4到n，按递推公式计算每一项；
 *   4. 输出数组第n项的值。
 */
#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    int a[22]; // 数组大小22，适配n≤20（下标1~20，避免越界）
    // 初始化前三项（核心，递推的基础）
    a[1] = 0;
    a[2] = 1;
    a[3] = 1;
    // 递推计算4~n项
    for(int i=4;i<=n;i++){
        a[i] = a[i-3] + 2 * a[i-2] + a[i-1]; // 严格匹配递推公式
    }
    cout<<a[n]<<endl; // 输出第n项
    return 0;
}

/* 可选优化写法（空间优化版，无需数组，空间复杂度O(1)）：
#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    // 边界处理：n≤3时直接输出初始化值
    if(n == 1){
        cout<<0<<endl;
        return 0;
    }else if(n == 2 || n == 3){
        cout<<1<<endl;
        return 0;
    }
    // 用三个变量存储前三项，替代数组（仅依赖前三项，无需存储所有项）
    int a1 = 0;  // Aₙ₋₃
    int a2 = 1;  // Aₙ₋₂
    int a3 = 1;  // Aₙ₋₁
    int an;      // Aₙ
    // 循环计算4~n项
    for(int i=4;i<=n;i++){
        an = a1 + 2 * a2 + a3;
        // 更新前三项：整体后移一位
        a1 = a2;
        a2 = a3;
        a3 = an;
    }
    cout<<an<<endl;
    return 0;
}
*/

/*
======= 核心重点 & 易错点 =======
【重点】
1. 你的代码核心逻辑完全正确（通过率低多为细节错误）：
   - 数组下标从1开始（贴合题目A₁/A₂/A₃的定义，易理解）；
   - 初始化前三项精准匹配题目定义（A₁=0、A₂=1、A₃=1）；
   - 递推公式严格无误（2×Aₙ₋₂是核心，漏乘2会导致结果错误）；
2. 空间优化逻辑：
   - 数列递推仅依赖前三项（Aₙ₋₃、Aₙ₋₂、Aₙ₋₁），无需存储所有项；
   - 用三个变量替代数组，空间复杂度从O(n)降为O(1)，n≤20时优化效果不明显，但n很大时优势显著；
3. 数据范围：
   - n≤20时，A₂₀的值为：计算得A₄=3、A₅=6、A₆=13…A₂₀远小于int最大值，无溢出；
4. 边界处理：
   - n=1/2/3时直接输出初始化值，无需进入循环（优化逻辑，减少冗余）。

【易错点】（通过率低的核心原因）
1. 初始化错误：
   - A₁初始化为1（后续所有项错误，如A₄=1+2×1+1=4，输出错误）；
   - A₂/A₃初始化为0（A₄=0+2×0+0=0，输出错误）；
2. 递推公式错误：
   - 漏乘2（a[i] = a[i-3] + a[i-2] + a[i-1]，A₄=0+1+1=2，输出错误）；
   - 下标写错（如a[i-2]写成a[i-1]，公式变成Aₙ=Aₙ₋₃+2×Aₙ₋₁+Aₙ₋₁，完全错误）；
3. 数组/循环错误：
   - 数组开太小（如int a[20];，n=20时a[20]越界）；
   - 循环i<n（漏算第n项，如n=4时只循环到i=3，输出a[4]未定义，结果错误）；
4. 变量类型错误：
   - 用short类型（n≥10时Aₙ超过short范围，溢出）。
*/