/*
 * 题目：四季
 * 来源：牛客网（noob28）
 * 知识类型：C++基础语法 | 数值拆分（取余运算） | 多条件分支判断（if-else if） | 字符串输出 | 区间判断
 * 题目原文：
 *   描述：根据六位整数YYYYMM（年份+月份）判断季节：3~5月spring、6~8月summer、9~11月autumn、1/2月winter。
 *   输入描述：一行输入六位整数YYYYMM（MM为01~12）。
 *   输出描述：输出对应季节英文名称（全小写：spring/summer/autumn/winter）。
 *   示例1：输入201901 → 输出winter；示例2：输入202506 → 输出summer。
 *   难度：入门，通过率52.08%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 从六位整数中拆分出月份（取后两位）；
 *   2. 按月份区间判断季节，输出固定小写字符串。
 * 解题思路：
 *   1. 用int接收六位整数n；
 *   2. 拆分月份：m = n%100（取后两位，如201901%100=1）；
 *   3. 多条件判断月份区间，输出对应季节。
 */
#include<bits/stdc++.h>
using namespace std;
int main() {
    int n,m=0;
    cin>>n;
    m = n%100;  // 拆分月份：取六位整数的后两位（YYYYMM % 100 = MM）
    
    // 按月份区间判断季节
    if(m>=3&&m<=5){          // 3~5月：春季
        cout<<"spring"<<endl;
    }
    else if(m>=6&&m<=8){     // 6~8月：夏季
        cout<<"summer"<<endl;
    }
    else if(m>=9&&m<=11){    // 9~11月：秋季
        cout<<"autumn"<<endl;
    }
    else{                    // 1/2/12月：冬季（题目描述1~2月，输入MM为01~12，12月也归为冬季）
        cout<<"winter"<<endl;
    }
    return 0;
}

/*
======= 核心重点 & 易错点 =======
【重点】
1. 月份拆分：用n%100提取后两位（核心技巧，如202512%100=12，正确归为冬季）；
2. 区间判断：用&&连接区间边界（如m>=3&&m<=5，不能写成3<=m<=5，C++不支持连续比较）；
3. 输出要求：字符串必须全小写（如Spring/SUMMER均错误），无拼写错误；
4. 边界覆盖：12月通过else归为冬季，无需额外判断。

【易错点】
1. 月份拆分错误：
   - 用n/100提取月份（如201901/100=2019，完全错误）；
   - 担心MM为01时m=1是否正确（题目中01~12对应1~12，取余后1/2/12均归为冬季，逻辑正确）；
2. 区间判断错误：
   - 写成3<=m<=5（C++中会先算3<=m，结果为0/1，再和5比较，逻辑错误）；
   - 漏判12月（else已覆盖，无需额外写m==12）；
3. 输出错误：
   - 字符串大小写错误（如Spring/WINTER）；
   - 拼写错误（如autumn写成autum、summer写成summor）；
4. 数据类型：无需用字符串接收输入，int拆分更高效。
*/