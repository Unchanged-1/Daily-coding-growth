/*
 * 题目：牛妹数
 * 来源：牛客网（noob25）
 * 知识类型：C++基础语法 | 条件判断（多条件逻辑） | 算术运算（奇偶判断） | 逻辑运算符（&&）
 * 题目原文：
 *   描述：牛妹数定义：整数是偶数 且 大于50（注：题目描述笔误，实际应为>50，示例验证）。给定n，判断是否为牛妹数。
 *   输入描述：一行输入整数n（1≦n≦100）。
 *   输出描述：是牛妹数输出yes，否则输出no。
 *   示例1：输入50 → 输出no；示例2：输入52 → 输出yes。
 *   难度：入门，通过率71.71%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 正确判断两个条件：n是偶数（n%2==0）且n>50；
 *   2. 输出固定字符串yes/no（小写，无多余字符）。
 * 解题思路：
 *   1. 用int接收n（1~100范围）；
 *   2. 多条件判断：n>50 且 n%2==0 → 牛妹数；
 *   3. 按条件输出yes/no。
 */
#include <bits/stdc++.h>
using namespace std;
int main (){
    int n;
    cin>>n;
    // 修正：题目核心条件是「>50 且 偶数」（你的代码此处逻辑正确，题目描述笔误为>5）
    if(n>50 && n%2==0){  
        cout<<"yes"<<endl;
    }
    else {
        cout<<"no"<<endl;
    }
    return 0;
}

/*
======= 核心重点 & 易错点 =======
【重点】
1. 条件逻辑：牛妹数需同时满足「>50」和「偶数」（用&&连接，两个条件必须都成立）；
2. 题目笔误修正：题目描述写的是「大于5」，但示例1（50→no）、示例2（52→yes）验证实际是「大于50」；
3. 运算符优先级：%（取余）高于&&，无需额外括号，逻辑清晰；
4. 输出格式：严格输出yes/no（小写，无空格/多余字符）。

【易错点】
1. 条件错误：
   - 把n>50写成n>5（匹配题目错误描述，导致示例1/2结果错误）；
   - 把&&写成||（只要满足一个条件就输出yes，逻辑错误）；
2. 奇偶判断错误：把n%2==0写成n%2==1（判断奇数，结果完全颠倒）；
3. 输出错误：输出YES/NO/Y/N（大小写错误，需严格小写）；
4. 语法错误：&&两侧漏空格（如n>50&&n%2==0，语法允许但可读性差，建议加空格）。
*/