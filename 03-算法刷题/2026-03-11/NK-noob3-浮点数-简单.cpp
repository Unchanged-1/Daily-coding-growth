/*
 * 题目：【模板】牛牛学说话之-浮点数
 * 来源：牛客网（noob3）
 * 题目原文：
 *   描述：会说整数之后，牛牛开始尝试说浮点数（小数）。输入一个浮点数x，输出该浮点数。
 *   输入描述：在一行上输入一个小数点后位数为6位的浮点数x (0≤x≤2)。
 *   输出描述：输出浮点数x；误差量级不超过10⁻³即被接受（|a-b|/max(1,|b|) ≤10⁻³）。
 *   示例1：输入1.359578 → 输出1.360；示例2：输入1.359578 → 输出1.359578000。
 *   难度：入门，通过率47.89%，时间限制1秒，空间限制256M。
 * 核心要求：
 *   1. 读取0~2范围内、小数点后6位的浮点数x；
 *   2. 输出x，允许误差≤10⁻³，无固定格式要求（保留3位/原始位数均可）。
 * 解题思路：
 *   1. 用double类型接收浮点数x（精度满足题目误差要求）；
 *   2. 直接用cout输出x即可，无需额外格式控制。
 */
#include <bits/stdc++.h>
using namespace std;
int main(){
    double n;
    cin>>n;
    cout<<n;
    return 0;
}

/*
======= 核心重点 & 易错点 =======
【重点】
1. 用double类型存储浮点数（float精度不足，double可满足10⁻³误差要求）；
2. 题目允许误差，直接输出即可，无需刻意格式化小数位数。

【易错点】
1. 误用float类型存储x（float精度约6~7位有效数字，可能触发误差超限）；
2. 过度格式化输出（比如强制保留6位小数，反而没必要，题目允许灵活输出）；
3. 输出时额外添加空格/换行（题目仅要求输出浮点数本身）。
*/